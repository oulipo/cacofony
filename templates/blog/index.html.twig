{% extends 'base.html.twig' %}

{% block title %}Liste des articles{% endblock %}

{% block body %}
<ul>
    {% for article in articles %}
    <li><a href="{{ path("detail", {'id': article.id}) }}">{{ article.titre }}</a>, catégorie: {{ article.categorie.libelle }} | 
    <a href="{{ path("editer", {'id': article.id}) }}">modifier</a></li>
    {% endfor %}
</ul>
{% endblock %}